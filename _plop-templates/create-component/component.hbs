import { cc } from 'utils/combineClasses'
import styles from './styles.module.scss'

interface Props {
  className?: string
}

const {{name}} = ({ className }: Props) => {
  return <div className={cc(styles.{{camelCase name}}Root, className)}>{{name}} component</div>
}


export default {{name}}
